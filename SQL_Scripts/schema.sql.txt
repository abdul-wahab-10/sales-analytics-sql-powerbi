CREATE TABLE orders (
    order_id VARCHAR PRIMARY KEY,
    order_date DATE,
    ship_date DATE,
    customer_id VARCHAR,
    customer_name VARCHAR,
    customer_segment VARCHAR,
    product_id VARCHAR,
    product_category VARCHAR,
    sales NUMERIC,
    quantity INT,
    discount NUMERIC,
    profit NUMERIC
);
